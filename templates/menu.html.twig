<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('ofertes') }}">JobOrWork</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link active" href="{{ path('ofertes') }}">Home <span class="sr-only"></span></a>
                <a class="nav-item nav-link" href="{{ path('empreses') }}">Empreses</a>
                <a class="nav-item nav-link" href="{{ path('admin') }}">Admin</a>
            </div>
        </div>
    </div>
</nav>